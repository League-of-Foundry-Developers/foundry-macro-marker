const CONSTANTS = Object.freeze({
    module: {
        name: 'macro-marker',
        title: 'Macro Marker'
    },
    hooks: {
        markerUpdated: 'macro-marker.markerUpdated'
    }
});

export default CONSTANTS;